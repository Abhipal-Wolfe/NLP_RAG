# Reflection Ablation: Self-reflection (Self-BioRAG)

generator:
  type: SelfBioRAGGenerator
  model_path: "dmis-lab/selfbiorag_7b"
  max_tokens: 200
  temperature: 0.0

retriever:
  type: FAISSRetriever
  top_k: 5

augmentations:
  query: []
  retrieval: []
  rerank: []
  generation: []
  reflection:
    - self_reflection:  # Validate and refine answer
        max_iterations: 2

dataset: "eval_datasets/medqa_train.jsonl"
max_samples: 100
output_path: "experiments/results/reflection_self"
